option(
  'profile',
  type: 'combo',
  choices: ['default', 'development'],
  value: 'default',
  description: 'The build profile. "development" uses a separate application id so it can be installed beside a release build.',
)

option(
  'visual_tests',
  type: 'boolean',
  value: false,
  description: 'Build the visual regression suite. Off by default: it needs an X server, ImageMagick and an installed gitg to compare against, none of which a package builder has. See spec 6.3.',
)
